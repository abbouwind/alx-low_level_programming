#include "main.h"

/**
 * add - adds two numbers
 * @a: the first num to add
 * @b: the second num to add
 *
 * Return: the sum of the two numbers
 */

int add(int a, int b)
{
	int c = a + b;

	return (c);
}
