#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: This function prints a specific message using printf
 * and adds a newline at the end.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
