#include <stdio.h>
/*
 * funtion main prints the following string "with proper grammar, but the outcome is a piece of art,"
 * followed by a new line using \n escape character as printf function requires it to add a new line 
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
